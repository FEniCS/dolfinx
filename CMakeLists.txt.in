cmake_minimum_required(VERSION 2.8.2)

message("HERE COMES SOURCE")
message("${DOLFIN_SOURCE_DIR}")
message("HERE COMES SOURCE")

project(googletest-download NONE)
   
  include(ExternalProject)
  ExternalProject_Add(googletest
    URL http://googletest.googlecode.com/files/gtest-1.7.0.zip
    SOURCE_DIR        "${DOLFIN_SOURCE_DIR}/GoogleTest/googletest-src"
    BINARY_DIR        "${DOLFIN_SOURCE_DIR}/GoogleTest/googletest-build"
    CONFIGURE_COMMAND ""
    BUILD_COMMAND     ""
    INSTALL_COMMAND   ""
    TEST_COMMAND      ""
    # Wrap download, configure and build steps in a script to log output
    LOG_DOWNLOAD ON
    LOG_CONFIGURE ON
    LOG_BUILD ON
  )