# CppHeaderParser depends on PLY, make sure it exists
try:
    import ply
except ImportError:
    raise ImportError('Could not import PLY. Make sure that PLY is ' \
                      'installed and its location is listed in PYTHONPATH.')

from CppHeaderParser import CppHeader
from utils import *
