# Copyright (c) 2005-2006 Anders Logg (logg@tti-c.org)
# Licensed under the GNU LGPL Version 2.1
#
# First added:  2005
# Last changed: 2006-03-28
#
# The bilinear form a([v q], [U P]) and Linear form L(v) for the
# Stokes equations using a mixed formulation (Taylor-Hood elements).
# This is the 2D version.

# Compile this form with FFC: ffc Stokes2D.form

P2 = FiniteElement("Vector Lagrange", "triangle", 2)
P1 = FiniteElement("Lagrange", "triangle", 1)
TH = P2 + P1

(v, q) = TestFunctions(TH)
(U, P) = TrialFunctions(TH)

f = Function(P2)

a = (dot(grad(v), grad(U)) - div(v)*P + q*div(U))*dx
L = dot(v, f)*dx
