# Things we need to do before building. Sort of a hack, but
# easier than figuring out how to set the dependencies with
# automake/autoconf

install:
	make -C ../lib clean
	mkdir -p $(prefix)/include/dolfin
