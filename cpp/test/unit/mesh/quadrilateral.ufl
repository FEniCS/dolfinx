# UFL input for the Quadrilateral test
# ==================================
#

element = FiniteElement("Lagrange", quadrilateral, 1)

coord_element = VectorElement("Lagrange", quadrilateral, 2)
mesh = Mesh(coord_element)

V = FunctionSpace(mesh, element)

u = Coefficient(V)

a = u * dx

# Before the form file can be used in the C++ program, it must be
# compiled using FFC by running (on the command-line):
#
# .. code-block:: sh
#
#    ffc Quadrilateral.ufl
